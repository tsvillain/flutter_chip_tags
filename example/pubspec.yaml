name: example
description: Example of flutter_chip_tags package.

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_chip_tags:
    path: ../

flutter:
  uses-material-design: true
